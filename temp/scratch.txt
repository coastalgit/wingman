wait. You may have two different scenarios. (1) We will be the same as this particular directory where we have .claude directory in the root, as well as two directories representing the legacy code (trunk) and new intended code (tm2). (2) we will be in the legacy code dirs, and then will be "copying" any directory from the "process" to another location WITHING the new project dir structure. 
