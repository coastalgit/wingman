---
phase: 01-terminal-pipe-poc
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - POC-06
  - POC-07

must_haves:
  truths:
    - "Slash commands typed in browser terminal (e.g. /help) work correctly"
    - "Interactive prompts (y/n confirmations, plugin menus) work correctly through the browser terminal"
    - "All 9 POC requirements verified manually by developer"
    - "Phase 1 is shippable — the PTY pipe is proven end-to-end"
  artifacts:
    - path: "server.js"
      provides: "Complete PTY pipe server"
      contains: "ptyProcess.onData"
    - path: "public/terminal.js"
      provides: "xterm.js browser terminal with full input/output/resize wiring"
      contains: "ResizeObserver"
  key_links:
    - from: "Browser terminal input"
      to: "Claude Code PTY stdin"
      via: "WebSocket input message + ptyProcess.write()"
      pattern: "ptyProcess\\.write"
---

<objective>
Human verification checkpoint: the developer runs `node server.js`, interacts with the Claude Code session in the browser terminal, and confirms all 9 POC requirements are satisfied. This is the phase gate before moving to Phase 2.

Purpose: POC-06 and POC-07 (slash commands and interactive prompts) are inherently interactive and can only be validated by a human using the running system. All other requirements (POC-01 through POC-09) are also confirmed in this single verification pass.
Output: Phase 1 declared complete. Developer confirms the terminal pipe PoC works end-to-end.
</objective>

<execution_context>
@C:/Users/BrendanFriel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BrendanFriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-terminal-pipe-poc/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all 9 POC requirements interactively</name>
  <files>none — verification only, no files modified</files>
  <action>
Human verification of the complete PTY pipe built in Plans 01 and 02. Run `node server.js` and interactively test all 9 POC requirements. No code changes — this is a checkpoint, not an implementation task.
  </action>
  <verify>
    <automated>MISSING — POC-06 and POC-07 require interactive human verification (slash commands and interactive prompts cannot be verified by an automated command)</automated>
  </verify>
  <done>Developer confirms all 9 POC requirements pass: server starts and opens browser (POC-01), xterm.js renders (POC-02), Claude Code spawns as PTY (POC-03), output streams in real time (POC-04), input reaches Claude Code (POC-05), slash commands work (POC-06), interactive prompts work (POC-07), ANSI renders correctly (POC-08), history is scrollable (POC-09).</done>
  <what-built>
Complete PTY pipe: node server.js starts a Node.js server, opens a browser window with xterm.js terminal, spawns Claude Code via node-pty in Git Bash, and bridges PTY output to the browser while sending browser keystrokes back to Claude Code.
  </what-built>
  <how-to-verify>
Run `node server.js` from the project root (D:\AI\2025-04_AspireHackathon\LIVE\UTILS\wingman\).

Check each requirement:

**POC-01** — Server starts and browser opens
- Expected: Console prints "Wingman running at http://localhost:7891", browser opens automatically

**POC-02** — Browser renders xterm.js terminal
- Expected: Dark terminal UI visible, blinking cursor, no blank/white page

**POC-03** — Claude Code spawned as PTY
- Expected: Claude Code banner/welcome message appears in browser terminal (not a blank cursor)

**POC-04** — Output streams to browser in real time
- Expected: Type "hello" — Claude Code's response streams character-by-character (not all at once after a delay)

**POC-05** — User input reaches Claude Code
- Expected: Claude Code responds to what you type (same test as POC-04)

**POC-06** — Slash commands work
- Expected: Type `/help` and press Enter — Claude Code's help output appears with formatting intact

**POC-07** — Interactive prompts work
- Expected: Trigger a y/n prompt (e.g., respond to a permission request or type a command that triggers confirmation). Press "y" or "n" — Claude Code registers the choice and continues.

**POC-08** — ANSI colours, spinners, formatting render correctly
- Expected: Claude Code output has colours (not raw escape sequences like `\x1b[32m`). Spinners animate.

**POC-09** — Terminal history is scrollable
- Expected: After enough output, scroll up with the mouse wheel or keyboard — previous output is visible (scrollback works).

If any requirement fails, describe the failure and type "issues: [description]".
  </how-to-verify>
  <resume-signal>Type "approved" when all 9 requirements are confirmed, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when the developer types "approved" after verifying all 9 POC requirements.
</verification>

<success_criteria>
- Developer confirms all 9 POC requirements pass interactively
- Slash commands work through browser terminal (POC-06)
- Interactive prompts (y/n, menus) work through browser terminal (POC-07)
- Phase 1 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-terminal-pipe-poc/01-03-SUMMARY.md`
</output>
