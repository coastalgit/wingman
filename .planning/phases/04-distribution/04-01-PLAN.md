---
phase: 04-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/wingman.js
  - server.js
autonomous: true
requirements: [DIST-01, DIST-02]
must_haves:
  truths:
    - "npx wingman starts the server and opens Mission Control in the browser"
    - "node bin/wingman.js works identically to node server.js"
    - "Git Bash is auto-detected across common Windows install paths"
    - "package.json has bin, files, and engines fields set correctly"
---

<objective>
Make Wingman installable and runnable via `npx wingman`. Add the bin entry point,
clean up package.json for publishing, and harden Git Bash path detection.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: bin entry point + package.json + BASH_PATH detection</name>
  <files>bin/wingman.js, package.json, server.js</files>
</task>

</tasks>
