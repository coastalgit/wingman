# Session Checkpoint — 2026-03-06 MC Theme

## Current Branch
`feature/mc-theme` (branched from `feature/phase5-session-ui`)

## What We Were Doing
Applying a new visual theme to Mission Control to fix card visibility (cards disappear into background).

## Task: MC Theme Redesign

### Theme Source
URL: https://tweakcn.com/themes/cmmeuka56000404la9tet5cnb
Theme name: WM1

### Dark Mode Tokens (what we need)
```
--background: #1c1917
--foreground: #f5f5f4
--card: #292524
--card-foreground: #f5f5f4
--primary: #b91c1c
--primary-foreground: #faf7f5
--secondary: #ea5d06
--secondary-foreground: #fef3c7
--muted: #1f1c1a
--muted-foreground: #d6d3d1
--accent: #b45309
--accent-foreground: #fef3c7
--destructive: #ef4444
--border: #44403c
--input: #44403c
--ring: #b91c1c
--font-sans: Poppins, sans-serif
--font-mono: IBM Plex Mono, monospace
--radius: 0.375rem
```

### What To Do
1. Rewrite `public/mission-control.css` design tokens using the WM1 theme above
   - Map `--bg-root` → `#1c1917` (background)
   - Map `--bg-base` → `#1c1917` (background)
   - Map `--bg-raised` → `#292524` (card)
   - Map `--bg-overlay` → `#292524`
   - Map `--bg-input` → `#44403c` (input)
   - Map `--bg-hover` → slightly lighter than card
   - Map `--primary` → `#ea5d06` (secondary/orange — matches existing orange brand)
   - Map `--text-1` → `#f5f5f4`
   - Map `--text-2` → `#d6d3d1`
   - Map `--text-3` → `#a8a29e`
   - Map `--border-1` → `#292524`
   - Map `--border-2` → `#44403c`
   - Map `--border-3` → `#57534e`
   - Map `--danger` → `#ef4444`
   - Map `--success` → `#8bc78b` (keep)
   - Map `--info` → `#7399bf` (keep)
   - Update `--font-sans` to `Poppins` (add Google Fonts import)
   - Update `--font-mono` to `IBM Plex Mono` (add Google Fonts import)
   - Update `--radius` to `0.375rem`

2. Session cards need much more contrast — currently blend into dark bg
   - Card background should be `#292524` (card token) with clear border `#44403c`
   - Active card border-left: orange (`--primary`)
   - Hover state: `#312e2b` (slightly lighter)
   - Selected state: brighter border

3. Add double-click on session card to open session (for non-active sessions)
   - In `mission-control.js` renderSessions(), add `dblclick` event to card
   - Double-click opens the session in new tab: `window.open('/session/' + session.id, '_blank')`

4. Also apply same font updates to `public/session.css` (Poppins + IBM Plex Mono)

### Files to change
- `public/mission-control.css` — full token rewrite + card visibility
- `public/mission-control.html` — update Google Fonts import URL
- `public/mission-control.js` — add dblclick handler
- `public/session.css` — update font tokens + Google Fonts import
- `public/session.html` — update Google Fonts import URL

### After changes
- Sync all changed files to testbed/
- Commit on `feature/mc-theme`

## Previous Session Work (completed, committed)
All on branch `feature/phase5-session-ui`:
- commit `0ecb9de` — Phase 5 UAT fixes (context per-session, FAB, delete btn, etc.)
- commit `e5ae661` — Prompt text typed directly to PTY + terminal height increase

## Key Architecture Reminder
- Source: `server.js`, `lib/`, `public/` (NEVER testbed)
- Testbed: copy source → testbed for UAT, user runs server
- MC polls sessions every 4s as fallback for missed WS events
- Context is per-session (stored in session JSON as `contextText`)
- Prompt sends text directly to PTY stdin (visible in terminal)
